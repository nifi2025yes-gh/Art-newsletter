# Hermes 프로젝트 다른 PC로 이사하기 (마이그레이션 가이드) 🚚

새로운 PC에서도 매주 월요일 아침 8시에 전 세계 미술관 소식을 받기 위한 이사 가이드입니다.

## 1. 기존 PC에서 할 일
- 이 폴더(`museum_newsletter`)를 통째로 복사해서 USB에 담습니다.
- 💡 **꿀팁**: 복사하기 전에 용량이 큰 `venv` 폴더는 삭제하고 옮기시면 USB 용량도 아끼고 오류도 방지할 수 있습니다. (새 PC에서 어차피 새로 만들어야 합니다!)

## 2. 새로운 PC에서 할 일
1. USB에 담긴 `museum_newsletter` 폴더를 새 PC의 원하는 곳(예: 바탕화면이나 D드라이브)에 복사합니다.
2. 새 PC에 **Python(파이썬)**이 설치되어 있는지 확인합니다. (없다면 설치 필수!)
3. 윈도우 검색창에 `cmd`를 치고 엔터를 눌러 명령 프롬프트(터미널)를 엽니다.
4. 방금 복사한 폴더로 이동한 뒤, 아래 명령어를 순서대로 한 줄씩 복사해서 붙여넣고 엔터를 칩니다.

```powershell
# 1. 새 PC 전용 가상환경 뼈대 만들기
python -m venv venv

# 2. 가상환경 접속하기
.\venv\Scripts\activate

# 3. 필요한 부품(라이브러리) 한 번에 모두 설치하기
pip install -r requirements.txt
```

## 3. 윈도우 작업 스케줄러 등록 (매주 월요일 아침 8시 자동화)
- 윈도우 검색창에 **'작업 스케줄러'**를 검색해서 엽니다.
- 우측의 **'기본 작업 만들기(Create Basic Task)'**를 클릭합니다.
- 이름: `Hermes_Museum_Newsletter`
- 트리거: `매주 (Weekly)` ➡️ `월요일 (Monday)` ➡️ 시간은 `오전 08:00:00` 설정
- 작업: `프로그램 시작(Start a program)`
- 프로그램/스크립트: 폴더 안에 있는 `run_newsletter.bat` 파일을 찾아(찾아보기 버튼) 선택합니다.
- 마침을 누르면 끝! 🎉

이제 컴퓨터가 켜져 있는 한 매주 월요일 아침 8시마다 전 세계의 최고급 전시 소식이 당신의 메일함으로 자동 배달됩니다.

---
*Created by Hermes & Antigravity (2026.07.09)*
